[package]
name = "gitmaster-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "gitMaster 独立业务核心，不依赖桌面框架"

[dependencies]
serde.workspace = true
cap-std = "=4.0.2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
