# 执行记录 — docs/m2-m3-supplement-spec-plan.md

当前状态：2026-09-24 按用户要求暂停功能开发并整理提交。下文按发生顺序保留，早期“尚未实现”或“进行中”不是当前汇总；请先阅读[暂停交接文档](m2-m3-supplement-handoff.md)。

## 2026-09-24

- 已核对当前 m2m3-fix 分支，基线 0474f55；开始时仅三份本次计划文档未提交。
- 基线 pnpm typecheck 通过。tests/m2-m3 在本机没有既有用例，旧通配命令运行 0 项，不视作测试通过。
- 任务 2 开始：新增选择方向、统一差异和分支引用展示回归；首次因新增模块不存在失败，开始实现对应模块。
- 共享契约：任务 2/8 共用 selected 与 prepareLocal；先保持现有 prepare/execute 校验链。任务 3/4/7 共用终端 profile 与会话生命周期，设置不可重启活跃会话。任务 5/6 共用仓库快照，草稿保存和同步都必须拒绝旧状态。
- 执行约定：未经提交授权，不执行技能模板中的自动 commit；进度与验证写在此文档，持续保留，不删除。当前目录开发位置偏好及新增依赖授权已询问；依赖尚未下载。

### 第一批实现

- 任务 2：已接入真实分支头、当前/分离 HEAD 标签、多引用展开；合并暂存按钮与提交按钮同行；混合方向禁用；点击文件展示二级统一差异抽屉。尚待原生视觉、上下文折叠与大文件验收，不能标记任务完成。
- 任务 3：控制器新增按任务聚合的有界 history；200 条终态、100 条事件，活跃任务保留，避免存储整份仓库快照。接入任务列表、详情、筛选和名称查询。多标签容器尚未实现。
- 任务 8 部分：暂存、取消暂存、创建分支直接执行原 prepare/execute 链，WriteConfirmation 对 immediate 操作不展示；仍检查过期及防重复。提交的完整索引核对、远程同步确认策略尚未调整。
- 前端新增本地测试三份，首次失败后实现：选择与 diff/引用 4 项、历史 4 项、直接动作 2 项。最后补充合并准备结果提示，防止把待提交合并描述为全部完成。
- 本批 pnpm build（含 typecheck）通过，96 模块；此前 9 项测试通过后新增第 10 项，最终结果见 verification 最新记录。未执行 Rust 测试或原生 UI 验收，无后端功能变更。
- 下一步：完成底部标签容器；安装授权到达后接入 PTY/xterm 与 Monaco；继续远程编排、文件编辑与设置迁移。当前工作留在 C:/code/GitMaster，未提交/推送。

### 第二批进行中：底部容器与统一设置

- 底部操作记录支持多个标签、关闭相邻选中、键盘切换、折叠和调整高度；非活动标签保留视图状态。终端入口仍禁用，等待真实 PTY 接入，不能视为终端功能完成。
- 设置后端新增 v3 完整模型、v1/v2 读取迁移、revision 并发保护、字段错误与全量应用 IPC。旧单项设置入口保留新增字段，校验失败不覆盖文件；保存终端配置不启动程序。
- 前端新增统一设置类型、IPC 服务、独立默认草稿、分类恢复、profile 删除约束和 useAppSettings。尚未替换旧设置界面，用户当前仍使用旧入口；下一步接入六类设置和脏草稿关闭确认。
- 系统默认终端 profile 的 executablePath 为 null，表示打开终端时自动发现系统 shell；不在默认配置中猜测用户机器上的可执行路径。
- 桌面设置专项 10 项通过；新增草稿专项先因模块不存在失败，实现后 2 项通过；严格类型检查发现数组首项可能不存在，改为显式校验后通过。
- 仍需完成终端、编辑器、远程同步以及原生交互验收，整体目标保持进行中；无提交、推送或依赖安装。

### 第三批进行中：设置界面接入

- SettingsPanel 替换旧分散设置表单，六类设置共用完整草稿和应用接口；提交图读取已保存偏好，日志信息在保存后重新读取。
- 新增多个终端 profile、独立参数条目、起始目录、显示参数、编辑器偏好和默认外部打开方式。设置可持久化不代表终端、编辑器和外部启动功能已实现，后续仍需分别接入。
- 关闭和重新读取前处理未保存草稿，可继续编辑、丢弃或保存并继续；失败时保留输入。Git 候选路径通过选择器进入草稿，应用后才切换环境；Git 检测期间禁止应用以减少环境竞争。
- 前端构建通过（101 模块）。原生模态焦点、系统路径选择器、多 profile 长表单及保存 IPC 的完整交互仍待验收，不宣称界面已验证完成。

### 差异上下文折叠

- 统一差异新增已加载长上下文折叠，两端各保留三行；展开后恢复原始文本与旧/新行号，增删行、hunk 和无尾换行提示不隐藏。
- 折叠状态随差异内容快照重置，解析通过 memo 避免每次展开重复解析文本。
- 新增两项回归先失败后通过，严格类型检查通过。此实现只处理后端已经返回的上下文；跨 hunk 缺失上下文补读、语法高亮、大文件与原生视觉验收仍未完成。

### 项目文件树接入

- 原平铺文件列表替换为左正文、右文件树，目录优先排序、可折叠、完整路径筛选；筛选时展开匹配祖先，保留原路径大小写和后端签发 fileId。
- 文件读取仍复用已有只读接口；没有新增编辑、保存、新建或删除入口。编辑器与外部打开仍待后续实现，不能将文件树接入视为任务 6 完成。
- 建树与筛选回归先失败后通过，严格类型检查通过；一万文件性能和原生视觉未验收。

### 远程同步前置：上游身份

- RemoteState 新增 branchUpstreams，使用 Git 原生解析字段返回本地分支、远端名称、目标引用和跟踪引用；remoteId 关联当前会话签发标识。避免从含斜杠的展示名猜测目标。
- 隔离仓库新增用例覆盖含斜杠远端、不同名目标分支、尚无跟踪引用；实现前编译失败，实现后通过。前端强类型契约同步更新并通过 typecheck。
- 同步执行前仍需完成配置唯一性/有效性复核、当前分支门禁、全分支 fetch、首次 upstream 设置和前端编排；目前仅完成只读映射，不提供新的网络写操作。

### 直接提交与页面索引版本保护

- 复现页面快照读取后同一文件重新暂存不同内容但状态字母不变，旧 prepare_commit 仍接受的问题；新增 Rust 回归在修复前实际失败。
- 仓库快照增加仅后端保存的完整索引条目摘要；读取状态前后核对索引，写入准备复核摘要，执行保留原完整指纹。增加两次有界只读查询，须后续验证大仓库读取成本。
- 提交按钮改为直接提交，页面继续提示提交包含全部已暂存文件；前端保留预检、一次性计划、防重复与过期拒绝。
- 新增索引过期专项修复后通过；前端直接动作 3 项通过、typecheck 通过。尚未执行完整桌面提交 UI 验收。

### 系统文件管理器入口

- 项目文件正文工具区新增“在文件管理器中打开项目”，复用已安装的 Tauri opener；前端只提交 repositoryId，后端校验会话、规范化当前根目录并打开。
- 不接受任意路径或命令；双击期间阻止重复启动，面板卸载后不发布迟到反馈，目录不可访问和启动失败使用明确中文提示。
- typecheck 和桌面 cargo check 通过。尚未实际启动系统文件管理器验收；VS Code/系统终端选项以及默认外部应用设置消费仍待实现。

### 外部打开选项接入

- 新增 VS Code、系统终端可用性查询与选择，读取已保存的默认外部应用；当前选择不隐式改变全局默认值，未找到应用时禁用对应选项。
- Windows 查标准用户/系统 VS Code 安装目录与系统 PowerShell；macOS 查标准 Applications 目录及 Terminal.app。自定义安装位置尚未覆盖，不自动下载或修改系统关联。
- VS Code 的项目路径为独立参数；Windows 终端用进程 current_dir 和新控制台启动，不把路径拼接为命令。外部进程退出后由后台等待回收。
- 前端 typecheck、Windows 桌面 cargo check 通过。新增特殊字符参数构造专项正在执行；未实际打开外部应用、未验证 macOS 启动。

### 编辑文档字节契约

- 独立 Rust 文件模块增加完整文本解码/编码契约：2 MiB 编辑上限、UTF-8 BOM 单独保存、原始正文换行不转换、内容摘要；二进制/错误编码/超限明确拒绝，不能保存截断预览。
- 两项专项覆盖空文本、仅 BOM、LF/CRLF/CR/混合换行及无尾换行的逐字节往返，另覆盖 NUL、非法 UTF-8、BOM 计入上限和版本摘要变化，均通过。
- 此模块尚未接入文件身份检查、原子保存或 Monaco；项目页面仍只读，编辑任务未完成。前端模型后续必须保留原始换行，不能依赖编辑器默认归一化。

### 完整编辑文档读取接入

- ProjectFilesSession 新增 read_editable，复用既有普通文件安全读取，结合仓库、fileId、路径、文件身份和完整内容生成版本；未知文件 ID、过期索引和不可编辑类型拒绝读取。
- 桌面新增 read_editable_file，预览和完整文档共享仓库队列、快照和文件列表 Arc 身份复核，仓库切换后的旧读取不能发布到新会话。
- 前端同步 EditableFile 契约与读取服务，新增中文文件编辑错误。完整文档已可通过 IPC 获取，但页面尚未使用，保存和编辑器仍待实现。
- 隔离仓库完整读取与外部修改版本变化专项通过（28.50 秒）。没有保存真实项目文件。

### 核心文件保存

- 新增 ProjectFilesSession.save_editable：校验仓库快照、签发 fileId 与完整文档版本，编码时恢复 BOM；无变化直接返回，不触碰文件。
- 复用冲突保存已有的同目录临时文件、权限保留、文件/父目录身份复核和原子替换机制，仅参数化完整读取上限；原冲突保存仍使用原 1 MiB 上限。
- 新回归先因保存方法不存在失败，实现后通过（23.73 秒）：实际保存只改变临时仓库工作文件，保留 HEAD、空索引与 BOM/CRLF，并拒绝旧版本覆盖外部内容。
- 尚未暴露桌面保存 IPC；必须先接入写任务生命周期与仓库切换门禁，不能把读队列接口直接当作完整写任务使用。编辑器仍未接入。

### 文件保存写任务接入

- 新增 SaveFile 任务类型与 prepare_file_save IPC，文件会话 Arc、仓库快照和桌面准备代次均复核后交付一次性计划，执行复用现有 execute_write、队列和状态查询。
- 核心准备与实际执行都校验文件版本；保存完成通过标准结果刷新仓库状态。前端控制器增加 saveFile 直接动作及“保存项目文件”记录名称，重复点击不会重复执行。
- typecheck 与桌面 cargo check 通过；前端直接动作 4 项通过。核心保存回归已改为通过实际协调器准备/执行，复测进行中。
- 编辑器模型、保存成功后草稿更新、多个文件标签及退出保护尚未接入，项目页面仍只读。

### 多文件草稿状态

- 新增编辑标签纯状态：按仓库内路径关联文档、独立草稿与已保存基线；脏标签关闭默认拒绝，需明确丢弃。
- 外部读取到不同内容时保留草稿并标记外部变化；确认保存只更新提交时的基线，保留保存期间的新输入，并要求重新读取文件版本后才能继续保存。
- 两项回归先失败后通过，typecheck 通过。该状态尚未接入页面或仓库切换门禁，不能当作完整多标签编辑功能交付。

### 全分支 fetch 核心草案

- 新增 FetchAll 请求，所选远端全部 heads 下载到私有临时仓库引用，下载参数禁止标签、prune、递归子模块和自动维护；成功后用 update-ref 事务比较旧 OID 并批量创建/更新跟踪引用。
- 发布前复核 HEAD、配置摘要和跟踪引用集合；符号引用撞名拒绝，网络失败不发布跟踪引用，标准任务结果记录刷新和不确定结果。
- 当前仅接受单 URL 与标准全分支 refspec；自定义映射、命名空间交叠与完整网络下载测试仍需补齐。顶栏刷新尚未接入，不能将此草案视为刷新功能完成。
- 两项解析/事务构造测试、核心 cargo check 和 typecheck 通过；真实事务过期拒绝与桌面编译复测进行中。

### 手动刷新入口接入（2026-09-24）

- 顶栏移除重复的新建分支和获取更新入口，刷新接入 FetchAll；左侧新建分支保持可用。
- 优先当前分支解析到的上游远端；只有一个且未解析上游时使用唯一远端，否则弹出远端选择页，不猜测 origin。无远端退回本地刷新。
- 复用直接任务准备/执行、防双击与过期计划门禁；页面显示准备阶段错误，自动及任务完成后的内部刷新仍不联网。
- typecheck 通过；直接任务回归 5 项通过，新增指定远端全分支获取、防双击、无二次确认和过期拒绝用例。
- 本地完整下载测试首次失败于 Git 对象写入。诊断确认测试替身缺少生产已有的 Windows 路径兼容转换，已对齐 dunce::simplified 后复测。
- 尚未完成：无远端提示、本地与网络结果分别呈现、特殊 refspec、实际桌面交互验收，以及同步远程与首次上游设置。顶栏旧上传入口将在同步流程实现时替换。
- 完整本地下载复测通过（106.89 秒），已验证多分支、无标签、本地文件/HEAD/索引不变及成功时间；前端生产构建通过。测试使用临时仓库，未访问用户远端。

### 刷新失败反馈补齐（2026-09-24）

- 无远端时显示“未配置远端，仅刷新本地”，本地读取失败仍使用仓库区域原有失败/旧状态提示，不把尝试刷新描述为成功。
- FetchAll 前端返回已启动、未启动或准备错误；准备失败时补读本地状态，已启动/结果未核实任务继续走既有终态流程，避免重复补读或重复执行。
- 远端列表读取失败同样允许独立读取本地。错误与提示按仓库会话保留，不因本地 snapshot 更新而消失；切换项目时清除。多远端选择仍在快照变化时失效。
- 直接任务回归 7 项通过，新增准备失败结果保留和执行响应丢失不误判用例。尚未进行原生界面失败场景验证，完整分别记录本地/网络结果及最后成功获取时间的展示仍待补齐。

### 同步远程上游解析（2026-09-24）

- 新增核心 RemoteSession.sync_target 与桌面 read_sync_target，前端同步定义强类型 SyncTarget。此接口只读，不联网、不写上游。
- 校验真实仓库快照、具名已提交分支、工作区/索引干净、无进行中操作；前后核对配置，旧远端会话或配置变化拒绝。
- 明确区分从未配置、完整上游和无法解析的配置。remote/merge 任一缺失、重复、指向本地仓库或非分支目标均不能伪装成首次设置。按完整跟踪引用关联后端 ID，允许含斜杠的远端名及不同目标分支，尚未 fetch 的已有上游仍保持 configured。
- 配置解析单测通过；真实临时仓库的未获取上游与外部配置变化回归通过（48.96 秒）；typecheck 和桌面编译通过。干净/残缺配置回归仍在运行。
- 此为同步入口的前置识别。按钮、首次设置上游、fetch 后评估及自动选择快进/推送尚未接入，不代表同步功能已经交付。特殊 refspec 的唯一映射和实际写入时复核仍需完成。
- 干净/残缺配置回归完成：1 通过（54.58 秒），包含未跟踪文件导致快照失效及脏状态拒绝。

### 当前分支同步推送路径（2026-09-24）

- 新增 SyncPush 请求，核心从当前分支已校验上游取得目标，前端不能在此请求中传入其他分支或远端。
- 复用普通非强制推送，增加贯穿准备/执行/网络查询后再验证的干净工作区与具名分支门禁；已配置目标在服务器缺失时要求重新设置，不静默新建。
- 普通手动推送保留原语义。快进现有 CapturedTree 已在写入前重验 HEAD、修改和进行中操作；自动同步快进的上游绑定还需接入。
- Windows 临时仓库回归已通过：准备后新增未跟踪文件导致推送拒绝，远端 main 与本地文件保持原状（79.83 秒）。测试传输替身对齐生产的 Windows 对象目录路径转换。
- 类型检查、桌面编译、中文错误归一化测试通过。正常同步推送及远端目标消失回归进行中；自动选择动作、同步按钮与首次设置上游仍未接入。
- 正常同步推送及目标缺失回归通过（88.42 秒）：临时 bare 仅 main 更新，目标不存在返回 SYNC_TARGET_MISSING，未创建额外分支。
- 新增 SyncFastForward 请求，由后端当前上游确定目标并复用快进计划；交付前再次核对配置和快照，执行沿用检出门禁。桌面编译通过，当前分支上游选择的真实临时仓库测试进行中。
- 同步快进真实回归完成（91.12 秒），验证上游目标不同名且不是列表首项时仍只推进当前分支；页面自动同步编排和首次上游配置仍待实现。

### 自动同步编排控制器（2026-09-24）

- 新增 remoteSyncController，依次读取上游、获取更新、等待终态刷新、重新签发远端目标并评估；领先使用 SyncPush，落后使用 SyncFastForward，一致仅提示，分叉停止。
- 每个异步阶段核对同一仓库、分支、源 HEAD、干净状态与上游名称；获取失败、目标变化、仓库切换及未知执行结果不会继续写入。无上游/目标缺失保留进入目标设置页所需状态，尚未实现设置写入。
- operationsController.runRemote 复用既有执行和轮询，等待终态回调后返回；恢复出的未核实记录不作为自动后续写入许可。useOperations 暴露该方法，useRepository 暴露同步读取当前快照的 getter。
- 类型检查通过，直接任务及同步编排测试共 17 项通过，覆盖四种关系、失败停止、切换分支停止、防双击、等待本地刷新及执行响应丢失。
- 页面按钮、无上游目标表单与完整原生流程尚未挂载/验收；这些控制器测试不代表完整同步功能完成。

### 设置当前分支上游任务（2026-09-24）

- 新增 SetUpstream 任务及请求，输入当前远端会话 ID 和目标分支名，复用一次性预览/执行及操作历史。
- 仅接受当前具名干净分支；目标跟踪引用必须存在，所选远端使用标准 fetch 映射且不能与其他已识别远端的 fetch 目标重叠。
- 准备绑定配置摘要和跟踪引用 OID，执行前复核；使用 Git branch --set-upstream-to 的参数数组，仅指定当前分支。执行后重新解析上游并核对目标，已尝试写配置但不能核实结果时保留 unknown，不自动回滚。
- 核心与桌面编译通过；强类型 TS 契约、任务中文名称已接入。临时仓库的配置写入/旧计划拒绝回归正在运行。
- 目标尚不存在时仍返回目标缺失；“先普通推送创建、核验后设置上游”及页面目标选择流程尚未完成。
- 上游设置回归首次通过（94.35 秒）；增加 branch.autoSetupRebase=always 与已有 branch.main.rebase=false 场景后复测通过（79.43 秒）。执行参数显式禁止自动设置 rebase，保留用户原设置。
- 新增 PublishBranch 请求与首次发布策略：只发布当前干净具名分支，服务器已存在目标时返回 TARGET_EXISTS，不能把“新建目标”转换为推进已有分支。发布成功不自动写上游，后续应再次获取并核验目标，再执行 SetUpstream。
- 待修正的重要评估边界：FetchAll 按要求不 prune，旧跟踪引用会保留。同步不能仅凭本地 tracking ref 判断目标仍存在；接入 UI 前需保存本次下载实际返回的 heads 清单，并用该清单识别已删除远端目标，避免 equal 误报同步成功。
- PublishBranch 回归通过（130.86 秒），验证服务器已存在 main 时拒绝首次发布，new-release 成功创建且 main 和本地 HEAD 不变。
- 已补充每远端 fetchedBranchNames 清单：仅全分支下载/发布引用核验成功后更新，配置未变时继承到新会话，配置变化时清除。同步解析及上游设置会拒绝清单中不存在的旧跟踪目标；相关扩展回归正在运行。
- 分支清单扩展回归完成：sync_target_preserves_unfetched_upstream_and_rejects_changed_config 通过（84.29 秒）；downloads_all_branches_without_tags_or_local_changes 通过（85.57 秒）。清单能继承至同配置的新会话，保留在本地但不在实际获取清单中的跟踪目标不再作为可同步分支。
- 相关前端测试通过 strict TypeScript 检查，git diff --check 通过。下一步仍为首次配置/发布的前端编排与同步按钮、设置页挂载。

### 同步入口接入与分步结果反馈（2026-09-24）

- 同步按钮、无上游目标设置弹窗与 React 控制器已接入；表单支持明确选择远端、目标分支及允许创建新目标，取消不写入。
- 首次设置先获取核验目标；新目标发布后再次获取，再设置当前分支上游并继续同步。分叉停止，提供现有合并流程入口。
- 同步期间暂停后台远端重签与自动刷新，其他写操作受忙碌门禁约束。
- 修正分步结果：发布或上游写入已成功但刷新失败时保留已完成事实；上游设置后同步失败也明确保留上游已设置提示，不继续自动写入。
- 16 项同步控制器与 9 项直接操作测试通过，pnpm typecheck 通过。完整原生界面与网络认证尚未验收；终端、编辑器及其余计划仍继续推进。

### 项目文件忽略项显示（2026-09-24）

- 核心新增可选忽略文件清单，默认不读取忽略项；前端开关经 IPC includeIgnored 传入，文件树单独标注“已忽略”。不修改 ignore 规则、不新增文件操作。
- 使用 Git ls-files 的忽略规则，已跟踪优先去重；所有 .git 路径分量不签发文件 ID。切换开关重新读取列表并清空旧正文路径，迟到响应沿用代次门禁。
- 临时仓库回归通过（33.77 秒）：默认隐藏、开启展示及读取内容、强制跟踪优先、旧列表 ID 拒绝、元数据不展示。
- pnpm build（含 typecheck）、cargo check -p gitmaster-desktop --tests --locked --offline 通过。大目录仍使用原 10,000 项上限；目录按需读取、编辑 UI 和原生界面验收尚未完成。

### 多文件草稿与保存控制器（2026-09-24）

- 新增 fileEditorController，按当前仓库路径管理多个完整文档；脏标签关闭进入待处理状态，未获丢弃选择不能切换仓库归属，迟到读取不会切换当前标签。
- 保存前使用当前快照文件清单重新读取文档，基线正文或 BOM 改变时拒绝覆盖；捕获提交文本，保存期间新输入保持脏状态；未知结果不确认保存。保存成功但刷新失败单独提示已保存。
- operationsController 增加 runFileSave，和自动同步共用真实终态等待，供后续编辑器 UI 在刷新完成后确认草稿。
- 修正编辑错误码在前端归一化中遗漏的问题，FILE_CHANGED、编辑限制和外部打开错误不再退化为泛化 Git 执行失败。
- 编辑与同步/直接操作回归共 35 项通过，typecheck 通过。当前仅完成控制器，尚未挂载 Monaco、多文件 UI 与窗口退出保护；依赖安装授权仍待用户答复。

### 刷新分阶段结果与时间展示（2026-09-24）

- 手动刷新接入 runRemote 的真实终态等待；页面分别显示远端获取与本地读取状态。准备失败仍可单独刷新本地；未核实任务不重试网络，保留未核实提示。
- 本地结果从仓库控制器实际 loading/stale/error 判定；读取排队中不误报成功，远端成功不因本地失败被改写。
- 工具栏与远端表单将后端 Unix 毫秒成功时间格式化为本地日期时间；无记录、元数据暂不可用与非法时间有不同文案，不以当前时间补值。
- 30 项刷新结果/直接任务/同步编排回归通过，strict 检查及 pnpm build 通过。普通浏览器预览确认顶部标签可读且无重叠；字号调整到 11px 后仍需复核。真实桌面网络失败交互尚未验收。

### 操作记录原始目标归属（2026-09-24）

- 执行前从计划捕获项目完整路径、源分支、目标分支/引用及文件摘要；之后进度和终态使用捕获值，不读取当前页面分支作为来源。下载记录保留目标目录，不存远端凭据 URL。
- 恢复没有目标摘要的旧任务时明确显示历史目标未记录；历史已有摘要继续保留。长路径列表省略且可悬停查看，详情允许断行。
- 直接操作 12 项、历史 4 项、同步 16 项共 32 项通过，含执行响应前切换分支与恢复记录不冒充来源。typecheck 和测试 strict 检查通过；多项目原生交互仍待验收。

### 前端完整回归与底部交互实测（2026-09-24）

- 当前 tests/m2-m3 全部 56 项通过，覆盖已落地的选择方向、差异折叠、分支标签、文件树、设置草稿、记录聚合、编辑控制器与同步编排。
- 浏览器操作确认：两个记录标签各自保留不同筛选，方向键切换同步焦点，折叠/再展开保留当前标签筛选。终端仍禁用，没有将记录标签验证当作 PTY 验收。
- 修复实测发现的分隔条辅助功能上限缺失：原 300px 被读取为 100，现显示实际高度并支持键盘 20px 调整。使用 ResizeObserver 在工作台变小时收回面板，为主区预留 240px。
- 1000×520 预览确认面板上限 147px，主区和底部筛选可达，恢复默认视口。pnpm typecheck 通过；真实任务列表、多平台原生尺寸和终端生命周期仍待验证。

### 设置默认配置与错误定位（2026-09-24）

- 对齐终端设置既定要求：删除默认 profile 前必须由用户明确选择替代项；按钮禁用并显示原因，纯状态函数也拒绝自动选择替代配置。更新此前自动选择替代项的测试。
- 校验摘要增加分类与终端配置序号，点击定位切换分类、滚动并聚焦对应字段，不保存或丢弃其他草稿。覆盖当前后端字体、字号、回滚、Tab、Git 路径及终端 profile 校验字段。
- 4 项设置回归通过，新增跨分类错误路由与多配置错误序号检查；typecheck 与测试 strict 检查通过。真实桌面错误聚焦、应用持久化与终端运行时配置仍待验收。

### 混合换行编辑边界与核心整体回归（2026-09-24）

- 对齐原计划：混合 LF/CRLF/CR 文件拒绝进入完整可保存模型，返回 FILE_EDIT_LINE_ENDING；普通文件预览路径保持可读，单一换行/BOM/无尾换行仍保留原契约。
- 前端增加独立中文只读说明和错误码归一化，边界提示回归通过；pnpm typecheck 通过。
- 已启动 cargo test -p gitmaster-core --locked --offline -- --test-threads=2，188 项核心测试运行中，尚无最终结果。该进程启动早于本次混合换行修改，完成后须补跑 editor_text 测试，不把旧二进制结果当作新改动验证。

### 编辑重新读取与核心失败待诊断（2026-09-24）

- 文件编辑控制器新增重新读取：脏文件需要明确放弃，读取成功前不清除草稿；失败、新输入、关闭和保存并发均保留正确文档状态，不发生文件写入。
- supplement-file-editor 共 12 项通过，typecheck 与测试 strict 检查通过；尚未挂载编辑 UI。
- 核心全量测试仍运行，create_supports_sha256_repository 已报 FAILED，但测试汇总尚未输出失败原因。保留原进程，并以原已编译测试二进制单独开启该用例的 nocapture 诊断；不能宣称核心全量通过，也尚无依据修改实现。

### 核心测试阶段更新（2026-09-24）

- 已确认 SHA-256 分支测试是测试辅助等待先于生产 120 秒预算退出；仅修正辅助等待并保留原断言。原全量运行已因明确的等待缺陷停止，未标记通过。
- 最新 editor_text 两项 Rust 单测通过；SHA-256 分支用例在修正等待后重新运行。其余失败仍须复核，完整核心回归尚未完成。

### 分支失败诊断结果（2026-09-24）

- 已复测确认 SHA-256 分支用例通过，原测试等待仅 10 秒而真实任务用时超过该值；等待改为生产 120 秒预算加交付余量，生产超时与成功断言未放宽。
- Rust 格式检查已修正并通过。分支模块其他用例和桌面设置持久化测试仍在运行；完整核心回归尚待重跑，不能将单例通过等同全部问题解决。

### 本地差异上下文补读（2026-09-24）

- 本地文件差异增加 contextLines 可选参数，默认 3 行；用户按 20/100/500/2000 档位读取更多真实上下文。仍只使用签发 changeId 和比较侧，不从前端接收文件路径。
- 核心拒绝超过 2000 行上下文，保留 1 MiB/5000 行返回上限；已截断结果禁用继续补读并沿用明确截断提示，已加载上下文可通过原折叠控件展开。
- 仓库控制器沿用请求代次：旧文件补读迟到不能覆盖后来选择的文件。相关刷新/乱序测试共 8 项通过，pnpm build、核心与桌面测试代码 check、Rust fmt 与差异检查通过。
- 新增临时仓库 expanded_context_reads_hidden_lines_and_rejects_excess 已编译，等待当前分支模块复测结束后执行，尚不能声明原生补读已验收。

### 补充前端完整回归与分支复测结果（2026-09-24）

- 最新 tests/m2-m3 共 65/65 通过，包含后续增加的设置错误定位、编辑重读保护和差异上下文请求竞态。
- 分支模块复测已结束：8 通过、1 失败、1 忽略，耗时 641.63 秒。剩余失败为 switch_is_readonly_until_execution_and_checks_actual_target 的成功结果断言，不能归因于之前已修复的测试等待问题。
- 给该断言增加实际 OperationResult 输出，保留原成功条件，待单独复现定位；生产代码未据此猜测修改。真实差异上下文临时仓库测试已开始运行。
- 真实 Git 上下文临时仓库测试随后通过（23.89 秒）：默认隐藏的上下文可补读，真实增删行保持正确，超过 2000 行参数被拒绝。原生差异界面和大型差异性能仍待验收。

### 用户要求暂停并提交阶段成果（2026-09-24）

- 已停止功能开发，新增 m2-m3-supplement-handoff.md 汇总需求决策、已实现源码、未完成内容、接口流程、已测结果、失败与待测清单、后续恢复顺序。
- 剩余分支单例已结束，准备阶段 TIMEOUT（169.35 秒），根因未定；没有继续修改生产逻辑或将其标为通过。
- 收尾重跑 pnpm build（含 typecheck）、cargo check -p gitmaster-desktop --locked --offline 通过。整理文档后 pnpm format:check、cargo fmt --all -- --check、git diff --check 通过。
- 用户本次授权提交和推送 GitHub；按当前 m2m3-fix 分支保存检查点，不合并 main、不发布。临时 tests/ 保持本地忽略，源码内 Rust 测试随代码保存。
