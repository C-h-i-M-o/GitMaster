# gitMaster M2/M3 工作台补充开发：需求与实施计划

日期：2026-09-24。当前状态：**恢复开发中，整体未完成，未通过整体验收。** 本轮从 GitHub `m2m3-fix` 的 `0dab490` 恢复；此前 `0474f55` 起的阶段开发及提交记录保留于[暂停交接文档](m2-m3-supplement-handoff.md)。当前证据以[补充开发进度](m2-m3-supplement-progress.md)和[验证记录](verification.md)的恢复章节为准。

本文件合并维护本次 spec 与 plan，完整目标不因阶段暂停或环境限制而缩减。本轮已获计划内源码、文档、测试及必要第三方依赖安装授权；xterm、FitAddon、portable-pty 与 Monaco 已接入。历史暂停收尾的提交/推送授权只适用于当时成果，本轮恢复开发不自行提交、推送、合并或发布，也不操作用户真实仓库的 Git 写入。

原生验证只记录实际完成项目。锁屏、缺失平台或认证环境导致未执行的测试保持待验证，不写作通过；macOS Release 构建成功不代表双平台或安装验收完成。

执行时先阅读本文与总计划第 12 节，使用 executing-plans 按任务推进；本轮整体批准后，未另行更改的设计建议按第 9 节固化的默认方案执行；发现范围外决策再确认。前端继续 React/strict TypeScript，Tauri 2 为桌面适配，独立 Rust 核心承载 Git 和文件业务；不引入数据库。

## 1. 现状、依据与范围

### 1.1 已核对的实现

| 模块     | 当前实现位置                                                    | 本次差异                                                     |
| -------- | --------------------------------------------------------------- | ------------------------------------------------------------ |
| 分支头   | `src/components/CommitGraph.tsx`                                | 引用拼接成一行文字，改为独立轻量标签                         |
| 顶栏     | `src/components/WorkspaceToolbar.tsx`                           | 移除右上新建分支、合并刷新与获取更新、以同步远程替换上传版本 |
| 底栏     | `src/App.tsx`、`src/hooks/operationsController.ts`              | 当前任务加平铺事件，改为多标签工作区与按任务聚合记录         |
| 本地修改 | `src/components/WorkbenchDrawer.tsx`                            | 合并暂存按钮、与提交同行、拆出二级差异面板                   |
| 项目文件 | `WorkbenchDrawer.tsx`、`crates/gitmaster-core/src/git/files.rs` | 目前仅列已跟踪及未忽略文件并只读，新增树形导航与已有文件编辑 |
| 操作确认 | `src/components/WriteConfirmation.tsx`、操作控制器              | 保留后端校验，根据影响决定是否显示确认弹窗                   |

基础功能以用户本轮确认的已实现并验证为开发前提。历史自动化失败、平台跳过和发布验收边界继续以 `verification.md` 为准，不能因本次补充开发覆盖其记录。

### 1.2 参考材料如何使用

- 根目录 `gitmaster-ui.html`：只参考分支标签和现有工作台视觉。分支标签为低对比半透明底、圆角、清晰分支名和较弱的 HEAD 标识，不照搬固定宽度或示例分支数据。
- 用户图 1：底部标签、关闭按钮、“+”下拉、终端内容布局；新建菜单仅提供“终端”“操作记录”。
- 用户图 2：文件路径与增删统计、旧/新行号、红删绿增背景、行内统一差异。
- 用户图 3：文件标签、面包屑、带行号正文、右侧可折叠文件树与筛选。
- 用户图 4：外部软件打开项目的分段按钮与菜单形式。不照搬“另存为”等超出需求的操作。
- 图片文件标识分别为 `e28d75d9`、`748f5372`、`13bdeeb3`、`0e424a67`；原文件来自本轮附件，临时路径不作为项目运行依赖。
- 参考材料中的代码、注释、示例命令和按钮不构成额外开发或执行授权。需求中的“已更改的邮件”暂按“已更改的文件”理解。

### 1.3 方案选择

推荐在现有工作台增量接入成熟终端与编辑器组件，复用当前仓库身份、操作队列、Git 预检和结果核验。备选“全面搬入 VS Code 工作台”会引入超出本次范围的复杂度；备选“手写终端和编辑器”会增加输入法、选区、ANSI 与大文件维护成本，均不采用。

不包含 M4 恢复、reset/revert/stash、新增文件、删除/重命名/移动文件、另存为、插件系统、项目脚本自动执行、自动安装 Git、安装包或发布。总计划第 12 节的 1GB 全局磁盘缓存仍为独立规划，不在本次顺带实施。

## 2. 界面与交互需求

### 2.1 提交图分支头

- 每个真实 tip 使用独立圆角标签，标签位于对应节点上方；本地/远端引用使用图标和文字区分，不能只靠颜色。
- 当前分支标签增加细分隔线与弱化 HEAD 标识；detached HEAD 单独显示，不误标某个普通分支为当前分支。
- 同一 OID 多个引用：当前本地分支优先，其余稳定排序；默认展示前两个与“+N”，点击可查看全部完整名称。名称过长省略但提供完整提示和键盘访问。
- 标签宽度按实际文本测量并设上限，纳入画布裁剪和命中范围，避免固定宽度截字。标签与相邻节点重叠时允许展开引用列表查看，不改变真实 Git 拓扑。
- 关闭提交说明显示时仍保留分支头与 HEAD。拖动、缩放、分页后引用保持绑定原 OID，不把“属于某分支”当作提交固有属性。

### 2.2 底部多标签面板

- 标签栏支持激活、关闭、横向滚动和“+”菜单；每次新建选择终端或操作记录。底部可折叠、拖动调整高度；折叠不关闭会话。
- 终端标签独立会话，标题含 shell 与项目简称；操作记录标签共享同一份数据，可保留各自筛选、滚动与选中任务，不重复启动任务轮询。
- 默认打开一个操作记录标签，不自动启动 shell。终端具体能力及项目切换策略见第 9 节；推荐各会话绑定创建时的项目，切换项目不偷偷改变旧 shell 的目录。
- 操作记录使用“任务列表 + 选中详情”：列表展示动作、项目/分支、状态、开始时间、耗时；运行中置顶，其他按时间倒序。状态包括进行中、已完成、失败、待处理、结果未知。
- 详情将 checking/transferring/verifying 等事件按阶段归组，展示目标、结果、可恢复提示与诊断编号；不把同一任务每次轮询重复显示为新操作。
- 支持全部/进行中/失败筛选和名称查找。无操作、加载中、失败为空、多个任务、任务结果未知分别呈现；结果未知只能重新查询，不能引导重复写入。
- 建议仅保留当前应用会话最近 200 条已结束任务，每任务最多 100 条阶段事件；活跃任务不因数量限制丢弃。达到上限移除最旧已完成记录并说明保留范围，不持久化命令正文或原始输出。该上限为设计初值，需在验收中测量。

### 2.3 顶部刷新与同步远程

右上保留“刷新”“同步远程”及设置，移除“新建分支”“获取更新”“上传版本”；左侧新建分支保留。

**刷新：** 用户明确点击时同时读取本地状态并执行 fetch；不改变工作文件、不合并、不推送、不 prune。自动刷新、操作完成后的本地状态更新和焦点恢复不得调用这条联网入口，防止隐式反复 fetch。fetch 范围按第 9 节 Q3 执行。

无远端时仍完成本地刷新并显示“未配置远端，仅刷新本地”。网络失败不阻止可用的本地结果展示：分别记录本地刷新与远端获取结果、最后成功获取时间。连续点击合并为一个任务，fetch 完成后失效引用/历史/远端评估并按需读取。

全分支 fetch 成功后按远端名称保存本次实际下载的分支名称清单（`fetchedBranchNames`，未观测为 null，空远端为 []）。该清单与保留的本地跟踪引用分开：不 prune 仍可保留旧引用，但同步目标不在本次清单中时按目标缺失处理，不能因为旧 OID 与 HEAD 一致就宣告已同步。相同配置的会话刷新继承清单；配置摘要变化则使观测清单及时间失效，下一次同步重新获取。

**同步远程：** 索引、工作区、未跟踪文件均为空，且有具名当前分支、无冲突或进行中的 merge/rebase 等状态时允许。未保存的编辑器草稿也阻止同步，提示先保存或放弃。禁用原因显示在按钮附近并支持键盘访问。

同步先获取目标最新信息，再核对工作区、当前 HEAD、分支、上游与配置，按新结果决定动作：

| 本地相对远端                                | 行为                                                 | 完成判定                                               |
| ------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------ |
| 一致                                        | 不执行写入，提示已经同步                             | 最新评估 equal                                         |
| 仅落后                                      | 显式 fast-forward-only 更新当前分支及工作区          | HEAD 与目标 OID 一致，工作区核验完成                   |
| 仅领先                                      | 普通非强制推送当前 HEAD 至该分支对应远端目标         | 重新查询远端目标确认写入；后续远端又变化时展示最新差异 |
| 既领先又落后                                | **已由用户确认：停止同步，提示通过现有合并流程处理** | 不自动 merge/rebase/push                               |
| 无上游、远端目标不存在或映射不唯一          | 不猜测 origin/同名分支；转目标设置引导               | 无上游时弹出目标设置页，规则见下文                     |
| detached/unborn、无共同祖先或缺对象无法评估 | 不执行同步并显示原因                                 | 不伪造成功                                             |

只对当前分支执行拉取或推送：禁用 force、mirror、全部分支和自动推标签。复用现有显式 refspec 与认证规则，不使用裸 `git pull` 让本地配置暗中决定 merge/rebase。远端变化导致拒绝时重新评估，不自动覆盖或重试推送。

同步推送复用已有普通推送传输与核验，但增加独立的同步策略：目标从后端当前分支上游解析，准备及每次执行校验均要求具名分支、无本地修改和进行中操作；已有上游目标在服务器消失时返回目标缺失，不能自动创建。旧手动推送保持允许上传已提交内容的原有语义。首次明确选择新目标的创建流程单独处理。

写请求增加不接受目标参数的 `syncPush` 与 `syncFastForward`，由后端读取当前分支上游。快进复用既有整合计划，交付前再次校验原配置与仓库快照，执行复用指纹及检出门禁；UI 仍须先完成 fetch 并按最新关系选择动作，不能将本地已有跟踪引用的评估当作已联网同步。

前端同步编排按 `checking → fetching → assessing → syncing → idle` 串行进行，任务必须完成且终态本地刷新结束后才能读取新远端会话及评估。获取前后绑定同一仓库、当前分支、HEAD 和上游名称；切换仓库/分支、出现本地修改、上游变化、获取失败或执行结果未核实均停止自动链，不自动重试写入。无上游及目标缺失进入设置状态，分叉进入可打开现有合并流程的提示状态。内部任务轮询继续复用既有控制器，不另起一套后台轮询。

无上游首次同步（用户已明确确认）：点击“同步远程”即弹出远端和目标分支选择页，明确显示“设置为当前分支上游并同步”；取消不写配置。后端校验当前分支、工作区及目标后，现存远端分支设置上游并重新评估同步；目标尚不存在时明确提示将创建远端分支，普通推送成功并核验后再设置上游。配置失败与网络结果分别展示，不能把远端推送成功误报成全部失败并诱导重复推送；设置上游仅限当前分支，不修改其他分支配置。

配置阶段使用独立 `setUpstream` 任务，输入当前远端会话 ID 与目标分支名；准备绑定仓库快照、完整配置摘要及目标跟踪引用 OID。执行前重复核验，使用 Git 自身的设置上游命令并读取实际结果，不拼接 shell；配置写入已尝试但无法完整核验时保留未知结果，不自动回滚或重试。现有目标必须对应所选远端的唯一跟踪映射，首次创建远端目标仍须先完成普通推送。

首次发布使用独立 `publishBranch` 请求：目标由用户明确选择，准备查询服务器后必须确认目标不存在；若已存在则返回 TARGET_EXISTS 重新评估。执行只普通推送当前干净具名分支的固定 HEAD，核验成功后再获取目标并设置上游，不因配置失败重发已成功的推送。

分叉提示保留“打开合并流程”入口；删除旧顶栏按钮不能使已实现的合并/冲突处理失去可达入口。

### 2.4 本地修改与二级差异面板

保留未暂存、未跟踪、已暂存三组；冲突继续独立入口。勾选状态与点击查看文件独立。

| 勾选状态                 | 合并按钮         | 是否可点                         |
| ------------------------ | ---------------- | -------------------------------- |
| 无选择                   | 暂存所选         | 否，提示先选择文件               |
| 仅未暂存和/或未跟踪      | 暂存所选（数量） | 是                               |
| 仅已暂存                 | 取消暂存（数量） | 是                               |
| 暂存来源与已暂存同时勾选 | 暂存 / 取消暂存  | 否，明确提示不能同时处理两种方向 |

- 同一 MM 文件的两侧分别计入对应选择，不能因路径相同自动合并勾选。刷新快照后清理失效选择。
- 提交说明位于底部；合并暂存按钮与提交按钮同一行固定可见。提交包含整个索引，不以勾选列表过滤提交，旁边显示真实已暂存数量。
- 点击文件时，从右侧展开第二层详情，保持第一层文件列表、选择、提交草稿和滚动位置。宽窗口并排，窄窗口第二层覆盖第一层，提供返回按钮；无需打开操作确认模态框。
- 第二层头部显示完整路径、比较侧、增删统计与关闭按钮。默认采用图 2 的统一行内 diff：旧/新行号、红色删除、绿色新增、上下文折叠、横向滚动和文本复制；未知统计不显示为 0。
- 未暂存比较 index→工作区，已暂存比较 HEAD→index；unborn 的已暂存比较空树→index，未跟踪显示新增内容。部分暂存、重命名、删除、空文件、无尾换行不得混淆比较侧。
- 此面板只读，不提供逐行暂存或编辑；二进制、不可解码和截断必须显式说明。快速换文件或暂存后迟到响应不能覆盖当前详情。

### 2.5 项目文件查看、编辑与外部打开

- 项目文件区域采用“左正文、右文件树”，顶部文件标签、面包屑、保存与外部打开入口；文件树可折叠、按路径筛选，文件夹先于文件。多个文件标签独立保存滚动、光标和草稿。
- 仅查看和编辑已有普通文本文件；支持手动保存、Ctrl/Cmd+S、撤销/重做、查找、行号、基本语法高亮。不提供新建、删除、移动、重命名、另存为、自动保存或运行脚本。
- 保存只写工作文件，不暂存、不提交。成功后更新文件版本并使相应本地修改/差异失效；失败保留草稿。
- 关闭脏文件、切项目、退出前提供保存/放弃/取消；文件外部变化时不覆盖草稿。保存前核对文件身份、原始内容版本与路径边界，变化时拒绝覆盖并允许重新读取；不自动合并草稿。
- 保留编码、BOM、换行和权限。首轮建议普通 UTF-8/UTF-8 BOM 且可无损往返的文本可编辑；其他编码、混合换行、符号链接、重解析点、子模块及超限文件提供只读说明与外部打开。具体文件覆盖范围见第 9 节。
- 大文件只读/差异性能继续遵循总计划第 12.6 节；完整编辑模型建议先以 2 MiB 为有界编辑初值，超过时只读，不把截断内容当完整文件保存。该限制不得降低正式版阅读目标。
- 外部打开的目标固定为当前项目根目录。建议首版支持系统文件管理器、已安装 VS Code、系统终端；其他软件不纳入首版外部打开列表。不照搬“Default app”含混语义，不增加另存为。
- 使用已有 opener 能力或受限原生启动适配，应用标识映射可信可执行程序，参数数组传入目录；前端不能提交任意 executable/arguments。未安装软件隐藏或明确禁用，不自动下载，不修改系统默认程序。

### 2.6 确认弹窗分级（按整体批准采用）

| 操作                                                       | 建议反馈                                                                                               |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 看历史/文件/diff、筛选、切换标签、刷新 fetch、外部打开目录 | 不弹确认；区域状态或短提示                                                                             |
| 整文件暂存/取消暂存、保存无外部冲突的已有文本              | 直接执行；保留后端准备与过期检查，完成后局部反馈                                                       |
| 新建分支                                                   | 输入表单的创建按钮即确认，不再二次弹窗                                                                 |
| 当前干净分支快进、普通当前分支推送                         | 建议点击“同步远程”即授权该次动作，按钮附近预先显示目标与方向；首次目标选择不能省略                     |
| 本地提交                                                   | 建议在本地修改页展示完整暂存范围、提交说明和“提交到本机”，点击即执行；需核对整个索引仍与用户看到的一致 |
| 切换分支、普通合并、冲突结果覆盖/保存、完成合并            | 保留清晰的影响确认；每个动作只保留一次有效确认                                                         |
| 丢弃未保存草稿、关闭有任务的终端、结束应用中活跃终端       | 保留明确确认，避免无提示丢失工作                                                                       |

“取消弹窗”只改变交互层，不取消 prepare→校验→execute→核验。预检后状态或方向改变则停止并更新界面，不能把原点击授权套到另一目标。失败在相关区域提供可操作原因，避免同一个错误同时出现在 toast、模态框、底栏三处。

### 2.7 设置功能完善（范围已确认）

用户已确认完善 Git 环境、外观、诊断日志，新增终端、文件编辑器、外部打开设置，并统一保存、默认值和错误反馈。

现状：`WorkbenchDialogs.tsx` 有常规、外观、诊断日志三类；`usePreferences.ts` 手动保存外观，`useLogSettings.ts` 立即保存日志；`src-tauri/src/settings.rs` 使用 v2 配置，已有迁移、串行读改写和损坏文件保护。复用现有存储，统一交互，不另建第二套设置系统。

| 分类       | 本次设置项                                                                        | 默认及生效方式                                                        |
| ---------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Git 环境   | 自动检测/手动选择 Git、路径/版本/来源、重新检测、安装官网                         | 自动检测；候选路径先验证，应用后切换；忙碌时说明原因，不自动安装 Git  |
| 外观       | 节点弹性 1–10、显示提交说明                                                       | 默认 6、开启说明；分支头和 HEAD 独立于说明始终显示；遵循系统减少动效  |
| 终端       | 多个 shell 配置、默认配置、启动参数、起始目录、字体/字号、光标样式/闪烁、回滚行数 | 启动项仅新终端生效，字体与光标可应用当前终端；详见下文                |
| 文件编辑器 | 等宽字体、字号、Tab 宽度、自动换行                                                | 默认系统等宽、14px、Tab 4、关闭自动换行；Tab 宽度不批量修改源文件缩进 |
| 外部打开   | 默认应用、可用应用列表、重新检测                                                  | 默认系统文件管理器；应用支持范围仍见 Q5，不修改系统默认关联           |
| 诊断日志   | 保留 auto/error/warn/info/debug/trace、有效级别、状态、日志目录及打开目录         | auto 保留既有开发/正式环境映射；统一应用后生效，不记录终端输入输出    |

终端设置：

- 支持多个 profile，每项含名称、可执行文件、独立启动参数列表与起始目录策略；可设默认，新建终端也可选择其他已保存 profile。可添加、修改、删除配置；删除配置不关闭已启动会话，删除默认项前需选择替代项。
- 默认 Windows 优先已安装 pwsh，否则 Windows PowerShell；macOS 使用可验证用户 shell。支持用户指定其他可执行程序，不限制终端内输入的命令。
- 参数逐项输入，由 Rust 按数组传入，不拼接 shell 字符串。配置检测和保存不会启动程序；实际启动前再次核对程序与目录，错误定位到字段。
- 起始目录支持当前项目根目录、用户主目录、用户选择的固定目录，默认项目根目录；无项目时回退用户主目录并显示实际路径。固定目录是终端设置，不受项目文件编辑根目录限制。
- 建议默认字号 14px、系统等宽字体、块状光标、闪烁开启、回滚 5,000 行；字号 10–32px，回滚 1,000–50,000 行。字号与光标保存后可更新现有视图，回滚仅新终端生效，避免静默裁剪当前输出；输出背压及字节上限仍然有效。
- shell、参数、目录修改只影响新会话，绝不自动重启运行中终端。当前标签保留其实际 shell/目录信息，不能因为默认配置变更而显示错误标题。
- 首版不新增环境变量/密钥管理器、启动脚本正文或终端内容持久化设置；用户可以运行自己的 shell 配置。普通启动参数是明文设置，表单提示不要填入密码/token，参数不进入诊断日志。

统一交互与存储：

1. 左分类、右表单，底部固定“应用”“取消”“恢复当前分类默认值”。分类切换保留整页草稿；无修改时应用禁用；字段错误可定位并支持键盘访问。
2. 应用全部已改分类，经后端统一校验后一次持久化，成功后更新运行态；失败保留原生效配置及草稿，不部分保存。明确标记“已生效”或“仅新终端生效”。
3. 恢复默认只改变当前分类草稿，应用后生效；不清除文件、日志、运行中会话及自定义 profile。恢复终端默认选择自动检测项，删除用户 profile 使用独立操作。
4. 关闭有未应用修改的设置时提供“应用并关闭 / 放弃 / 继续编辑”；无修改直接关闭。目录选择、软件检测、打开目录属于辅助动作，不意味着应用草稿。
5. Git 路径选择不再立即保存，日志不再即时保存；Git 切换沿用仓库/活跃操作限制。各类设置统一通过同一持久化服务，避免旧 setter 覆盖新字段。
6. v1/v2 迁移至 v3，保留 gitPath、uiPreferences、logLevel，新增项填默认；读取不静默覆盖损坏或未知新版本配置。保存使用 revision 检查拒绝过期草稿，防止跨窗口覆盖。
7. 复用原有应用配置路径、锁和安全替换；不写仓库配置，不使用前端 localStorage，不持久化父进程环境或终端输入输出。

不增加主题商城、云同步、快捷键重绑定、Git 身份编辑器或尚未实现的缓存清理开关。分类及本节默认方案纳入本轮已批准的实施基线。

## 3. 技术方案与权限边界

### 3.1 组件选型

终端建议评估 xterm.js 与 FitAddon，Rust 端评估 portable-pty；编辑器优先评估 Monaco 的有界文本模型与本地 Worker，差异展示复用后端真实 patch 数据。依赖版本、许可证、包大小和 WebView 兼容性在获安装授权后锁定，不在本轮下载。

- [xterm.js 插件说明](https://xtermjs.org/docs/guides/using-addons/)：用于成熟终端展示与尺寸适配。
- [xterm.js 流量控制](https://xtermjs.org/docs/guides/flowcontrol/)：用于设计输出背压，不能无界堆积 IPC 输出。
- [Monaco 官方说明](https://github.com/microsoft/monaco-editor/blob/main/README.md)与[ESM/Worker 集成](https://github.com/microsoft/monaco-editor/blob/main/docs/integrate-esm.md)：用于评估编辑器复用；不引入整套 VS Code 或扩展市场。
- [portable-pty 官方文档](https://docs.rs/portable-pty/latest/portable_pty/)：作为 Rust 跨平台 PTY 候选，不能以文档存在替代 Windows/macOS 实机验证。

编辑器、Worker、终端资源均随包提供，不依赖 CDN、Vite 开发服务器或联网运行。

### 3.2 已确认的完整终端能力与边界

用户已明确确认终端允许任意命令并支持终端设置，为旧 AGENTS 与 M2/M3 的“无任意命令接口”规则增加独立终端例外，不再重复询问该项授权。用户主动打开 shell 并输入命令；Git 按钮仍走原受限接口，不转为 shell 字符串。本次已获开发批准，功能尚未实施。

推荐默认 Windows 优先已安装 pwsh，未安装时使用系统 Windows PowerShell；macOS 使用可验证的用户登录 shell。PTY 仅向当前窗口签发 sessionId，支持输入、输出、resize、退出；禁止仓库内容触发创建终端或自动输入命令。

shell 具有用户本机权限，应用不能保证其命令经过 Git 队列或危害确认；终端与外部编辑器产生的改动按外部变化处理。Git 按钮写入前仍重新检查快照，遇到锁或变化拒绝。不要宣称可对任意 shell 命令提供全仓库互斥。

终端输出留在内存终端缓冲，不进入诊断日志/操作历史持久存储；不记录输入，禁止输出转 HTML 或自动执行 OSC 指令打开应用、写剪贴板。建议每终端 5,000 行回滚、最多 8 个活跃终端，输出通道设字节上限与背压；达到限制显示原因。关闭会话释放 PTY、子进程和订阅，不能留下无主 shell。跨平台子进程树回收必须实测。

### 3.3 文件与任务边界

Git/文件保存使用独立 Rust 核心，桌面层负责 IPC 和系统打开。PTY 的系统实现放桌面独立模块，不让核心依赖 Tauri 或窗口。

文件保存使用后端签发 documentId 与版本令牌，验证当前仓库与路径，不接收任意绝对路径。采用受控替换与写前复核，处理 Windows 文件占用及失败清理，保留原文件；承认应用锁无法锁住所有外部工具，不宣称可消除所有外部竞争。

任务历史以 operationId 聚合。仓库切换后当前视图不接受旧会话结果，但已发起任务可以更新其所属项目的记录；关闭记录标签不取消 Git 任务。复用一个操作数据源，避免每标签创建重复轮询。

## 4. 拟新增或调整的数据契约

以下是设计接口，具体实施状态见进度记录。TS 与 Rust 同步强类型定义；所有时间使用毫秒时间戳，所有 ID 为后端签发的不透明字符串，枚举不以自由文本代替。

远端只读状态增加 `branchUpstreams`：每项含 `branchName`、`remoteName`、可空 `remoteId`、`targetRef` 和 `trackingRef`。映射由 Git 的上游格式字段解析，不按显示名称中的斜杠拆分；目标跟踪引用尚不存在时仍允许返回已解析配置。此字段只提供观测信息，不授予写入权限；同步执行前仍须在后端校验配置完整性、唯一性和快照时效。不能把未解析到映射直接认定为用户从未设置上游。

同步入口增加 `read_sync_target(repositoryId, snapshotId)`：复用当前远端会话，返回 `SyncTarget { repositoryId, snapshotId, branchName, upstream }`。upstream 区分 `missing`（remote/merge 均无配置）、`configured`（remoteId、targetBranchName、可空 remoteBranchId）及 `unresolved`（配置不完整、重复、指向本地仓库、非分支目标、远端或映射缺失）。跟踪引用尚未下载不等于没有上游。该接口只读，不联网；调用前后复核真实仓库快照与配置，并要求具名已提交分支、工作区和索引干净、无进行中操作。它只提供同步入口目标，不能代替 fetch 后评估及写入前再次校验，也不能授权覆盖 unresolved 配置。

直接提交前，仓库快照须在 Rust 内部保存完整索引条目摘要（不传前端）。状态读取前后比较 `ls-files --stage -z` 结果，读取期间变化则拒绝生成混合快照；提交准备时比较页面快照与最新摘要，状态字母相同但 blob 已替换也返回过期。执行阶段继续沿用完整写入指纹校验。额外只读查询纳入原总期限，大目录性能需重新测量。

| 数据                  | 字段与规则                                                                                                                                                                            |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BottomTab             | tabId、kind（terminal/operations）、repositoryId；terminalSessionId 仅 terminal 分支存在；筛选与滚动为前端视图状态                                                                    |
| TerminalSession       | sessionId、repositoryId、displayCwd、shellLabel、state（running/exited/failed）、exitCode（可空）                                                                                     |
| TerminalOutput        | sessionId、sequence、字节块；按顺序解码跨块 UTF-8，关闭后丢弃迟到事件                                                                                                                 |
| OperationHistoryEntry | 现有 handle/kind/progress/result，加 displayTarget、startedAt、finishedAt（可空）、有界阶段事件；所有目标描述脱敏                                                                     |
| RemoteSyncAssessment  | repositoryId、snapshotId、branchId、headOid、upstreamRefId、remoteId、targetOid、ahead、behind、fetchedAt、decision（equal/fastForward/push/diverged/blocked）、blockedReason（可空） |
| SelectionAction       | stage/unstage/none/mixed；分别携带 stageIds 与 unstageIds，mixed 不生成写请求                                                                                                         |
| DiffDocument          | 仓库快照、changeId、side、路径、旧/新路径、统计（可空）、hunks、truncated/unsupported；hunk 行含 context/add/delete、oldLine/newLine（可空）、text                                    |
| TextDocument          | documentId、repositoryId、fileId、version、path、text、encoding、bom、lineEnding、editable、readonlyReason（可空）；截断文本永远不可保存                                              |
| SaveDocumentResult    | 新 version 与文件元信息、状态刷新结果；保存成功而刷新失败需分别表达                                                                                                                   |
| ExternalApplication   | appId（受支持枚举）、displayName、available；不暴露可由前端替换的执行路径                                                                                                             |

| 接口（拟议）                                      | 输入 → 输出                                                          | 约束                                                |
| ------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------- |
| create_terminal                                   | repositoryId、shellId → TerminalSession                              | 当前窗口能力绑定，cwd 后端解析                      |
| write_terminal / resize_terminal / close_terminal | sessionId + 输入字节 / cols、rows / 结束请求 → 确认                  | 不接受命令行模板；尺寸与输入大小有界                |
| terminal_output / terminal_exit                   | 后端事件 → 对应会话                                                  | 有序、背压、注销订阅                                |
| list_operation_history                            | cursor、limit、repositoryId（可空） → entries、nextCursor            | 本会话有界历史，当前控制器仍管理任务执行            |
| refresh_repository_with_fetch                     | repositoryId、已确认目标策略 → OperationHandle                       | 内部各步骤复用 fetch/读取；不能成为所有刷新共用入口 |
| prepare_current_branch_sync                       | repositoryId、snapshotId → 带 assessment 的计划                      | fetch 后重新评估，目标绑定，分叉返回阻断            |
| execute_write                                     | 沿用 repositoryId、planId → OperationHandle                          | 计划固定目标/方向/HEAD/配置，过期则拒绝             |
| read_editable_document                            | repositoryId、snapshotId、fileId → TextDocument                      | 只读打开，不因读取创建文件                          |
| save_document                                     | repositoryId、documentId、expectedVersion、text → SaveDocumentResult | 已存在文件、版本匹配、可无损保存；排队              |
| list_external_applications / open_project_in_app  | 无参数 / repositoryId、appId → 可用列表 / 启动结果                   | 目标固定为项目根，不接收 shell 字符串               |

差异解析优先保留现有 FileDiff 兼容消费，在展示层转换有界 patch；如果现有返回不足以正确取得行号/分页，增量扩展核心响应，不能用错误的前端统计伪造完整差异。

### 4.1 设置契约增补

- `TerminalProfile`：profileId、name、executablePath、args（string 数组）、cwd（project/home/fixed 判别联合，fixed 带路径）。仅用户保存的配置可用于启动。
- `TerminalPreferences`：defaultProfileId、profiles、fontFamily、fontSize、cursorStyle（block/bar/underline）、cursorBlink、scrollbackLines。
- `EditorPreferences`：fontFamily、fontSize、tabSize（2/4/8）、wordWrap（off/on）。不新增自动编码/换行转换选项。
- `AppSettings`：version=3、revision、gitPath、uiPreferences、logLevel、terminal、editor、externalOpen（defaultAppId）；revision 为后端签发的不透明版本令牌。
- `read_app_settings() → AppSettings`；`apply_app_settings(expectedRevision, draft) → { settings, effectiveState }`。失败返回稳定 code 与 fieldErrors（字段路径/中文原因），不部分保存。
- `detect_terminal_profiles() → 可用程序候选列表` 只检测不启动。`create_terminal(repositoryId 可空, profileId) → TerminalSession` 替代前表 shellId 输入，后端按已保存目录策略解析；无项目终端的 repositoryId 可空。
- 旧设置接口调用点迁移或委托同一服务，禁止旧路径/日志 setter 继续写 v2 丢失新增字段。配置保存和运行态切换串行协调，不能持久化成功但默默沿用失效 Git 环境。

## 5. 文件职责与修改范围

| 任务            | 修改现有文件                                                                                                                                                                                                                                    | 拟新增文件                                                                                                                                                                                         |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 分支标签        | `src/components/CommitGraph.tsx`、`src/hooks/useCommitGraph.ts`、`src/ui/graphLayout.ts`、`src/styles.css`                                                                                                                                      | `src/ui/branchHeadPresentation.ts`                                                                                                                                                                 |
| 底部工作区      | `src/App.tsx`、`src/hooks/useWorkbench.ts`、`src/hooks/operationsController.ts`、`src/ui/operationPresentation.ts`                                                                                                                              | `src/components/BottomPanel.tsx`、`TerminalPanel.tsx`、`OperationHistoryPanel.tsx`；`src/hooks/useBottomPanel.ts`、`useTerminal.ts`；`src/services/terminal.ts`、`src/types/terminal.ts`           |
| PTY 桌面能力    | `src-tauri/src/lib.rs`、`src-tauri/src/commands.rs`、桌面依赖与能力配置                                                                                                                                                                         | `src-tauri/src/terminal.rs`、`src-tauri/src/commands/terminal.rs`                                                                                                                                  |
| 同步与刷新      | `WorkspaceToolbar.tsx`、`WorkbenchDialogs.tsx`、`src/hooks/useWorkbench.ts`、`remoteController.ts`、`src/services/git.ts`、`src/types/git.ts`；核心 `git/remote.rs`、`git/remote/fetch.rs`、`git/remote/push.rs`；桌面 `commands/operations.rs` | `src/hooks/syncController.ts`、`crates/gitmaster-core/src/git/remote/sync.rs`                                                                                                                      |
| 本地修改与 diff | `WorkbenchDrawer.tsx`、`ContentPreview.tsx`、`useWorkbench.ts`、`src/styles.css`                                                                                                                                                                | `src/components/ChangeDetailDrawer.tsx`、`UnifiedDiff.tsx`、`src/ui/diffPresentation.ts`、`selectionAction.ts`                                                                                     |
| 文件编辑        | 核心 `git/files.rs`、类型定义导出；桌面 `commands/readonly.rs`、`resources.rs`                                                                                                                                                                  | `src/components/ProjectFilesPanel.tsx`、`FileEditor.tsx`；`src/hooks/useFileEditor.ts`、`src/services/files.ts`；`crates/gitmaster-core/src/git/files/write.rs`、`src-tauri/src/commands/files.rs` |
| 反馈分级        | `WriteConfirmation.tsx`、`useOperations.ts`、`operationsController.ts`、`useWorkbench.ts`                                                                                                                                                       | `src/ui/confirmationPolicy.ts`                                                                                                                                                                     |

表中省略目录的组件均在 `src/components/`，hook/controller 均在 `src/hooks/`。新增模块按明确职责拆分，不顺带重构全部工作台。TSX 只保留 UI 与方法导入；事件处理、业务判断、状态副作用写在 TS。每个手写函数/组件与 Rust 公共接口增加中文职责注释。

设置专项修改 `src/components/WorkbenchDialogs.tsx`、`GitEnvironmentPanel.tsx`、`LogSettingsPanel.tsx`、`src/hooks/usePreferences.ts`、`useLogSettings.ts`、`useWorkbench.ts`、`src-tauri/src/settings.rs` 及现有设置 IPC；拟新增 `src/components/SettingsPanel.tsx`、`TerminalSettingsPanel.tsx`、`src/hooks/useAppSettings.ts`、`src/services/settings.ts`、`src/types/settings.ts`。复用已有原生选择器；移除因本次统一替换而失效的旧方法，不保留双套写入口。

## 6. 分步实施计划

### 任务 1：确认边界与固定契约

- [x] 第 9 节已记录逐项确认和整体批准后采用的默认方案。
- [ ] 获开发及缺失依赖安装授权后完成版本/许可证/离线打包评估，更新相关锁文件；终端权限例外同步 AGENTS、接口与安全文档。
- [ ] 固定第 4 节契约，整理操作记录归属、快照过期及编辑器脏状态的统一阻断规则。
- [ ] 验收：所有六项需求均有交互、契约和测试责任；无重复执行入口；现有 Git 按钮权限未泛化。

### 任务 2：分支标签与本地修改面板

- [ ] 先补引用排列、选择动作 none/stage/unstage/mixed、统一 diff 行号解析用例。
- [x] 实现标签布局、合并按钮、提交同行与二级抽屉，复用现有真实 diff 请求。
- [ ] 验收：多引用/长名称/HEAD、MM 双侧、重命名/删除/无尾换行、快速换文件、小窗口与键盘操作均符合第 2 节。

### 任务 3：底部标签与操作记录

- [x] 先补任务聚合、重复序号、跨仓库迟到事件、200 条淘汰与活跃任务保留测试。
- [x] 抽出共用底部容器和记录数据源，接入任务列表/详情、筛选、标签关闭和面板伸缩。
- [ ] 验收：两个记录标签展示同一真实任务且筛选独立；关闭标签不取消任务；多个连续操作结果均可回看。

### 任务 4：真实终端

- [ ] 先测试会话归属校验、关闭/退出、流控、尺寸与错误映射；在专用临时目录做原生 PTY 验收。
- [ ] 接入成熟终端、PTY 和有界输出通道，验证中文输入、复制粘贴、Ctrl+C、全屏程序、中文空格路径。
- [ ] 验收：两个终端独立运行；折叠不丢进程；持续输出不冻结主界面；关闭后进程与订阅释放；日志不包含输入正文。

### 任务 5：刷新与当前分支同步

- [ ] 使用临时工作仓库与本地 bare 远端覆盖 equal/ahead/behind/diverged、无上游、目标消失、远端竞争、工作区变脏与身份切换。
- [x] 复用已有 fetch、普通 push、快进与 coordinator，新增同步编排；移除冗余顶栏入口并保持合并入口可达。
- [ ] 验收：刷新只 fetch 不改工作文件；同步只影响当前分支；分叉停止；失败不自动重推；本地刷新成功与 fetch 失败可分别展示。

### 任务 6：文件树、编辑与外部打开

- [ ] 先补版本冲突、文件删除/被替换、路径逃逸、BOM/CRLF、只读/二进制/截断拒绝保存、文件占用失败用例。
- [x] 实现右侧文件树、多文档状态和手动保存，接入受限外部软件启动，保持已有冲突编辑流程独立。
- [ ] 验收：保存后系统文件内容正确且索引未变化；外部修改后草稿不被覆盖；脏状态的关闭/切换可取消；无新增/删除/另存为入口。

### 任务 7：设置整合与迁移

- [ ] 先补 v1/v2→v3、未知版本/损坏配置保留、分类字段互不覆盖、revision 冲突与持久化失败用例。
- [x] 实现统一设置草稿与应用流程，保留 Git/外观/日志能力，接入终端、编辑器与外部打开偏好；旧即时保存入口改用同一服务。
- [ ] 覆盖非法路径、无效字号/回滚、默认 profile 删除、程序卸载后启动失败、参数含空格/中文、无项目目录回退；保存/检测配置不能启动用户程序。
- [ ] 验收：重启后配置一致；取消不改生效值；多分类应用不部分保存；现有终端不因设置变化重启；关闭设置的草稿提示只出现一次。
- [ ] 验收：小窗口底部应用按钮可达，原生路径选择器与设置页焦点恢复正确。

### 任务 8：弹窗策略与整体回归

- [ ] 按第 2.6 节调整准备/执行控制流，增加直接动作的防重复、过期计划和提交索引变化测试；保留严重影响确认。
- [ ] 执行前端与 Rust 检查、原生工作流和视觉矩阵；记录测量环境与各模块资源上限。
- [x] 更新 `interfaces.md`、`ui-design.md`、`architecture.md`、`release-security.md`、`testing.md`、README 与 `verification.md` 的真实状态。未通过项不能记为完成。
- [x] 未获得单独授权时，不提交、不推送、不发布。

## 7. 验收矩阵与命令

| 关注场景                             | 必须核验的结果                                                            |
| ------------------------------------ | ------------------------------------------------------------------------- |
| 远端在准备后推进、HEAD/上游变更      | 旧计划拒绝，普通推送保护生效；不操作其他分支                              |
| 终端或外部编辑器并发修改             | 界面标记失效、按钮写入重新校验、保存拒绝过期版本；不宣称 shell 被队列控制 |
| 文件同时具有 staged/unstaged         | 两侧差异准确，混合选择禁用；提交仍包含完整索引                            |
| 连续切换仓库、文件和任务标签         | 旧响应不污染新视图，任务仍记录到正确项目，草稿不静默丢失                  |
| 大文本、长行、高频终端输出、多条记录 | 输出与模型有界、视图不持续冻结；关闭释放资源；限制有可见说明              |
| 860×620、200% 缩放与键盘             | 双层面板有返回路径，提交同行可访问，标签溢出可滚动，无焦点陷阱            |
| 浏览器预览                           | 原生能力显示不可用，不假成功，不执行 shell                                |

工程检查：`pnpm typecheck`、`pnpm build`、`pnpm format:check`、`cargo fmt --all -- --check`、`cargo test -p gitmaster-core --locked --offline`、`cargo check -p gitmaster-desktop --locked --offline`。增加桌面测试覆盖终端能力及文件 IPC；Windows 资源敏感测试按已知情况串行诊断，不扩大生产超时隐藏失败。

前端控制器/纯函数沿用本地 `tests/m2-m3/` 的 Node 测试方式，建议新增 `supplement-selection.test.ts`、`supplement-history.test.ts`、`supplement-sync.test.ts`、`supplement-editor.test.ts`；长期纳入仓库与否另行确认。Rust 核心测试就近放入已有模块测试，Git 写入仅用临时仓库与临时远端。

Windows/macOS 分别验证真实终端、外部打开、编辑保存和远程流程；`pnpm tauri build --no-bundle` 后验证资源/Worker 不依赖开发服务器。不能以构建、浏览器或单平台通过替代双平台与真实认证验收。正式版阅读性能沿用总计划：10MB/10 万行文本、1 万行差异、1 万文件；记录打开耗时、滚动响应和内存，不以编辑上限代替阅读验收。

## 8. 初次方案确认记录（历史）

- 已只读核对源码、HTML、历史开发与验证文档；未运行参考 HTML 的脚本，未执行真实 Git 写入。
- 未修改功能代码、依赖和 AGENTS；未运行功能测试，本轮仅做文档格式与差异检查。
- 本文件已获用户确认并批准开发；恢复开发后，未勾选的任务复选框表示该条尚未完成全部实现与验收核对；部分实现和已测证据见进度/验证记录，不据此断言功能完全未实施。

## 9. 已确认决策与实施授权

Monaco 接线细化：完整编辑模型仍受后端 2 MiB 上限；只导入本地编辑器 Worker 与基本语言高亮，不启用 CDN、项目运行、链接自动打开或扩展宿主。模型内部 LF 与磁盘换行通过纯函数双向转换，保留无末尾换行。已打开标签在抽屉隐藏时保留模型与视图状态；保存调用既有 fileEditorController/SaveFile，并在每次写后重新签发文件列表供下一份草稿保存。导航保护统一覆盖切项目、改变 Git 环境、克隆后切换以及窗口退出，不只保护标签关闭。

恢复开发设置校正：终端分类“恢复默认”只恢复显示偏好并选中系统自动检测项，保留全部用户配置及其参数/目录；若系统项已删除或编辑为自定义程序，则新建独立系统项，不能覆盖同标识配置。此项落实第 2.7 节既有要求，不增加设置范围。验收覆盖配置保留、标识冲突、重复恢复幂等与其他分类草稿不变。

终端原生路径选择：新增 `choose_terminal_path(kind: shell | directory) → string | null`，复用已安装的原生 dialog 插件。只选择已有程序或固定目录，不运行程序、不保存设置。结果仅更新发起选择时的草稿；期间编辑、重新读取、应用或关闭导致草稿变化时丢弃迟到结果，取消不清空字段。原生窗口交互在可解锁后补测，当前验证草稿更新和竞态门禁。

PTY 输出传输细化：使用 `read_terminal(sessionId, acknowledgedSequence)` 有界拉取代替无背压广播事件；前端完成 xterm.write 后才确认序号并请求下一块，响应丢失可重取未确认块。每会话输出队列 16×4 KiB、单次返回最多 16 KiB，输入队列 16×4 KiB；队满停止读取 PTY 或拒绝新增输入并明确反馈，不无限积累。会话绑定创建窗口和原项目，最多 8 个；关闭断开队列并结束 PTY，Unix 同时终止前台与 shell 进程组，Windows 关闭 ConPTY。用户显式脱离终端的守护进程不作为可保证回收的终端子任务；普通前后台任务清理必须测试，跨平台未测项保留待验收。

本地差异上下文补读：默认每处变化前后 3 行，用户可按 20、100、500、2,000 行逐级加载；`read_file_diff` 增加可选 `contextLines`，核心拒绝超过 2,000 的请求并继续保留字节/行数上限。补读绑定原文件、比较侧和仓库快照，切换文件或刷新后迟到结果作废；已截断结果不宣称完整上下文。

核心分支测试等待采用生产操作预算加有限终态交付余量，不能在任务仍允许执行时提前判定失败。只修正测试等待，不放宽生产超时、成功结果或仓库状态断言；已确认使用错误等待时限的全量运行须记录中止原因并重新验证。

编辑器重新读取：脏文件须先明确放弃草稿；旧草稿保留到磁盘读取成功。读取失败、读取过程中继续输入、切换项目或关闭标签均不覆盖原草稿；重新读取不执行文件写入。保存开始后使在途重新读取失效，避免成功保存的基线被旧读取结果替换。

编码边界实施：混合 LF/CRLF/CR 的文件继续通过只读预览查看，完整编辑文档入口返回 `FILE_EDIT_LINE_ENDING` 并说明使用外部编辑器，不把会统一换行的编辑模型用于此类文件。原 BOM、单一换行与无尾换行仍按字节往返验证。

设置校验反馈：错误摘要标明分类及终端配置序号，点击后切换分类并聚焦对应控件；输入修改后沿用统一草稿错误清理。删除默认终端配置前必须由用户先选另一默认项，界面与状态更新函数均拒绝自动替换。

底部面板尺寸验收：分隔条辅助功能同时提供当前高度、最小值和随工作台变化的最大值；窗口缩小时将现有面板高度收回可用范围，不遮挡主工作区。

操作记录归属：执行前捕获预览对应的项目路径、源分支及可用目标摘要，后续进度沿用该值，不从当前页面分支反推。恢复的旧任务若没有已捕获目标，只显示仓库身份及“历史目标未记录”，不伪造来源；已有记录继续保留原摘要。

手动刷新反馈：区分远端获取（未执行、进行中、成功、失败、未核实）与本地读取（未执行、进行中、成功、失败）。等待本次获取任务及终态回调完成后展示真实结果；准备失败可单独读取本地，未知任务不重复发起获取。最近成功获取时间仅来自后端会话元数据，失败不更新，界面转换为可读本地时间。

编辑控制器实施：多文件草稿按当前仓库路径归属；保存前从最新清单取得文件 ID 并重新读取完整文档，基线或 BOM 变化时拒绝覆盖。保存只确认已核实成功结果提交的文本，保存期间的新输入继续保留；成功后刷新失败须提示文件已保存。关闭脏文件等待保存/放弃/取消选择，仓库切换不能直接清除脏草稿；异步读取迟到结果不改变当前标签。

项目文件显示开关：`read_project_files` 增加可选 `includeIgnored`（默认 false），通过 Git 获取被忽略文件并标记 `ignored`；已跟踪优先去重，任何 `.git` 路径分量始终排除。切换开关重新签发文件 ID 并丢弃迟到读取结果，不改变 Git 忽略规则。当前列表上限保持 10,000，目录按需读取另行落实；验收包括默认隐藏、开启展示、强制跟踪文件不误标和忽略文件内容读取。

同步分步结果要求：发布或设置上游的后端结果为成功时，即使后续本地刷新或自动同步失败，也保留已完成步骤的明确提示；后续失败停止自动链，不把已成功写入描述成未发生。验收覆盖发布成功但刷新失败、上游设置成功但刷新失败，以及设置完成后获取失败。

用户已明确确认：分叉时停止同步并引导现有合并流程；终端允许任意命令并可配置；完善六类设置；无上游分支点击同步远程时弹出目标设置页，允许设置上游。随后用户确认开发文档并批准开发。

下表区分逐项明确答复和整体批准后采用的文档默认方案，不把默认方案描述成逐项答复。

| 编号 | 决策                                                                                                                                                                       | 依据                                   |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| Q1   | 完整交互终端允许任意命令，完善 Git、外观、终端、编辑器、外部打开、日志设置                                                                                                 | 用户逐项确认                           |
| Q2   | 无上游时点击同步弹出远端/目标分支设置页，确认设置上游后继续同步；取消不写配置                                                                                              | 用户逐项确认                           |
| Q3   | 手动刷新获取当前上游对应远端的所有分支；无上游且仅一个远端时取该远端，多远端无唯一目标时选择远端；无远端仅本地刷新。仅更新跟踪引用，不 prune、不自动取标签；内部刷新不联网 | 整体批准后采用文档建议并明确无上游行为 |
| Q4   | 默认已跟踪及未忽略文件，可切换显示忽略文件并按需展开；Git 元数据始终排除，不提供编辑                                                                                       | 整体批准后采用文档建议                 |
| Q5   | 首版外部打开支持 VS Code、系统文件管理器、系统终端；不增加 Git Bash/WSL 专用入口或自定义外部编辑器。终端 profile 仍支持用户指定 shell                                      | 整体批准后采用文档建议                 |
| Q6   | 按第 2.6 节实施确认分级，提交及普通同步明确范围后点击执行                                                                                                                  | 整体批准后采用文档建议                 |
| Q7   | 标签在当前应用会话内保留并标明原项目，重启不恢复 shell、未保存草稿和操作记录                                                                                               | 整体批准后采用文档建议                 |

开发授权覆盖本计划的源码、文档、测试与必要重构；依赖下载/安装遵循项目单独授权要求。测试仅用临时仓库；不沿用历史提交、推送、发布或跳过测试授权。当前批准的是开发，未将功能实现或验收标记为完成。

### 目录分页实施细化（2026-09-24）

保留已有 Git 生成的有界项目文件会话（10,000 文件、每次 Git 输出 8 MiB）。本步骤将目录响应和前端渲染改为按需获取，不声称底层 Git 索引或磁盘扫描完全按需。目录能力由后端签发，仅允许查询当前会话中的目录；前端不传文件系统路径。

- 新增 `read_project_tree(repositoryId, snapshotId, includeIgnored) → ProjectTreePage`，建立会话后只返回根目录第一页。原平面接口暂保留给既有合同测试，产品后续切换到新入口。
- `read_project_directory(repositoryId, snapshotId, treeId, directoryId, offset) → ProjectTreePage` 复用同一会话、文件与目录 ID，不因展开目录作废已打开文件。每页最多 200 项，目录先于文件，返回 nextOffset/total。
- `search_project_files(repositoryId, snapshotId, treeId, query, offset) → ProjectTreePage` 在同一清单按路径过滤，最多 256 字符查询，每页 200；返回原 fileId，不创建另一组写入身份。
- 页面字段为 repositoryId/snapshotId/treeId/directoryId/entries/nextOffset/total；条目区分 directory{id,path,name} 与 file{id,path,name,status}。刷新、切项目和忽略开关建立新会话，旧 treeId/目录 ID 拒绝。
- 前端按会话缓存已展开目录，折叠保留缓存，搜索使用独立分页视图，迟到响应不覆盖新会话；打开文档的保存仍须在新快照重新绑定 fileId。大目录先分页；列表虚拟化和大型内容的独立性能验收仍按第 12.6 节继续完成。
- 验收：真实临时仓库验证层级/排序/中文空格/忽略开关、超过 200 项分页无重漏、搜索沿用 fileId、伪造或过期身份被拒绝。桌面请求必须沿用 Context、协调器和 Arc 发布校验，不能因新增只读接口绕过会话门禁。

### 文件树视口虚拟化（2026-09-24）

复用 TanStack Virtual 的 React 适配层，不手写滚动测量器。已批准本任务所需第三方依赖安装；版本和许可证以实际安装结果记录。依据官方 useVirtualizer/getItemKey/rangeExtractor/scrollToIndex 接口实现：固定 32px 行、上下预渲染、路径与条目种类组合稳定 key；展开后的目录页扁平为可见逻辑行，分页入口与加载状态也作为行。DOM 只保留视口及当前键盘活动项，不能把一万行全部挂载。

键盘在滚动容器上采用单一焦点与 aria-activedescendant，支持上下/Home/End、左右展开/折叠及 Enter 激活；每行报告层级，分页动作保持可操作。过滤或会话变化后过期活动项回落到首项，保留有效路径焦点；隐藏面板不销毁滚动容器。正文、草稿与目录文件 ID 协议不变。验收以一万项独立夹具检查 DOM 上限、末项导航/激活、键盘焦点、折叠及分页，不将浏览器测量等同原生 WebView 性能。

### 本地变更列表虚拟化（2026-09-24）

沿用已安装的 TanStack Virtual，将未暂存、未跟踪、已暂存三组的组头/空组说明/文件行合并为固定高度逻辑行，只渲染视口与活动项。稳定 key 必须包含比较侧和 changeId，MM 同文件两侧不能合并。勾选仍调用原 toggleChange，查看仍调用 inspectChange，不增加隐式暂存或提交。

列表使用单一焦点的 grid，原复选框和文件按钮保留鼠标操作；上下/Home/End 移动活动文件，Space 勾选、Enter 查看。禁用写入或子模块时 Space/复选框不能改变选择；查看仍允许。活动项保留在 DOM，避免滚动回收破坏读屏引用。组计数、冲突入口、提交说明与提交范围说明继续由原工作台维护。

验收：一万变更项的 DOM 上限与首末导航；同 ID 的 staged/unstaged 同时存在、勾选互不干扰、查看不勾选；禁用/子模块边界；虚拟化不能改变原 prepare/execute、混选拒绝或提交包含完整已暂存索引的行为。浏览器夹具不替代原生万文件实际项目验收。

### 大文件只读分块阅读的实施约束（2026-09-24）

现有项目预览在 diff.rs 限制为 1 MiB/5000 行，编辑器完整模型限制为 2 MiB；后者继续保持，不能把放宽预览上限或整文件加载 Monaco 当作分块阅读完成。下一步按以下顺序实施，并以真实 10 MiB/100,000 行文件验证：

1. 从现有 read_regular 提取可复用的安全普通文件打开/读取前后身份核验，保留每个祖先、符号链接、重解析点、嵌套仓库和特殊文件的拒绝规则；不另写更弱的文件打开路径。先跑既有文件与冲突读写测试，确保提取不改变保存合同。
2. Rust 后台建立有界 UTF-8 行索引，采用流式扫描和独立文件句柄，不把完整正文送入 JS，也不把未提交正文落入持久缓存。记录 BOM、LF/CRLF/CR/混合换行、文件版本和总行数。读取前后核对身份/长度/时间与当前目录项，发生变化使旧文档失效，不能拼接不同版本。
3. 新只读文档 ID 绑定原项目文件能力和快照；页请求按逻辑行范围获取内容，限制单次响应字节。超长行必须显式分段并提供继续读取，不以截断内容冒充完整行。关闭或切项目释放句柄/索引，文档数与内存必须有界。
4. 前端使用已安装虚拟器渲染可见行并预读相邻页，缓存页有容量上限；支持行号、跳转、查找、可复制当前文本及超长行分段提示。普通受支持小文件保留现有 Monaco 编辑，分块文档只读且没有保存入口。
5. 大型差异另行扩展有界摘要/hunk 响应与后台解析，不能把完整巨型 diff 返回后在 JS 假分页。原三方冲突保存、文件身份和混合比较侧边界继续保持。未实现项不提前标记验收完成。

只读分块接口细化：首轮支持普通 UTF-8/UTF-8 BOM 文件，流式识别 LF/CRLF/CR 与混合换行。索引上限 64 MiB / 1,000,000 逻辑行，覆盖 10 MiB/100,000 行目标；超过上限明确拒绝索引并保留外部打开。行索引和 64 KiB 块摘要留在 Rust 内存，不保存正文副本或持久缓存；块摘要用于拒绝元数据未显著变化但正文块已改变的读取。

文档元数据含 documentId/fileId/path/byteLength/lineCount/bom/lineEnding。页请求接受 documentId、零基 startLine、1..200 count；单行分段最多 16 KiB，页面总文本最多 256 KiB。每行返回一基 lineNumber、text、byteOffset、nextByteOffset；长行可通过单行页的 byteOffset 继续读取，UTF-8 字符不从中间截开。正文行文本不混入换行符，换行类型在文档元数据明确显示。空文件与末尾换行的逻辑行遵循编辑器语义（空文件一行，末尾换行产生末尾空行）。

每个项目文件会话最多持有 8 个只读文档；刷新树/切项目使旧 ID 无效并释放无引用文档。读页仍在已有仓库读队列和 Context/Arc 发布门禁内执行，打开与读页前后检查安全目录项和文件身份。关闭只读文档不写任何文件。错误区分二进制、编码不支持、超出索引上限和文件已变化。

只读查找使用 `search_read_document(documentId, query, startLine, count)`：大小写敏感的单行字面量，UTF-8 查询不超过 1024 字节，结果每页最多 100 个不同的一基行号及 nextLine；扫描验证原块摘要、支持跨块命中，不构造完整正文字符串。前端首版用于“查找下一个”定位逻辑行，空查询与含换行查询拒绝，不引入正则执行或项目级搜索。

分块阅读界面实施：编辑入口因超过 2 MiB 或混合换行拒绝时自动切换只读分块视图，普通小文件仍用 Monaco。只读视图仅保留当前文件，切文件释放原句柄；迟到打开响应必须主动关闭。视口缓存最多八页，每页沿用 256 KiB 上限，页请求串行并优先当前可见范围，短页按实际返回范围继续读取。长行续读替换当前段而非无限拼接，提供回到首段；可见文本支持系统选择复制。查找下一个与跳转均定位逻辑行，错误后停止自动请求，用户显式重新读取才新建文档。验收包含旧响应清理、短页无空洞、缓存容量、长行、十万行虚拟 DOM 与跳转查找。

原生退出验收修复（2026-09-24）：Release 中双草稿 Cmd+Q 直接终止。已核对当前依赖源码，macOS 默认 PredefinedMenuItem::Quit 绑定 AppKit terminate:，Tao 只处理 applicationWillTerminate，不能靠现有 RunEvent::ExitRequested 在这条路径前确认。改为应用菜单中的普通“退出 gitMaster”项及 Cmd+Q，加上自定义菜单事件统一请求 window.close，由现有草稿/终端确认决定是否关闭。保留默认编辑/窗口等菜单。另补齐 onCloseRequested 在允许关闭后调用 destroy 所需的主窗口 allow-destroy 权限；不开放其他窗口或通用执行权限。验收必须在 Release 重测双草稿取消/放弃/保存及终端关闭，不能只依赖编译。系统强制结束不在可保护范围；Dock 正常退出另行核对。

macOS PTY 后台任务回收修复（2026-09-24）：Release 的交互 zsh 将后台 sleep 放在独立进程组；现有关闭只杀前台组和 shell，后台进程会被托管给 PID 1。补充真实交互 zsh 回归；复用成熟 macOS 进程枚举库，关闭时在 shell 尚未回收、会话 ID 仍受控的锁区内枚举并终止属于该终端会话的普通后台进程，再回收 shell。保留不同终端会话和主动脱离会话进程，不扩展为按名称或全系统杀进程。先验证复现失败与测试自身清理，再实现并复测多终端隔离、前后台任务、饱和输出和原生关闭。Windows ConPTY 生命周期仍按各自平台独立验收。

### 大差异分块实施契约（2026-09-24）

复用当前 Git 执行器、比较侧校验、literal pathspec 与 --no-ext-diff/--no-textconv。旧 FileDiff 预览接口的 1 MiB/5,000 行行为保持兼容；新增只读差异文档在 Rust 后台获取最多 16 MiB/100,000 原始 patch 行并建立索引。正文只在 Rust 留一份有界不可变快照，不落盘、不送入前端整段解析。超过上限明确标记截断，增删统计仅代表索引内完整内容，不冒充完整文件统计。

核心文档摘要包含行数、hunk 数、增删数与 truncated；正文行携带 kind、旧/新行号及文本。页请求零基 startRow、1..200 count，每页最多 256 KiB；单行每段最多 16 KiB，长行通过 byteOffset/nextByteOffset 显式续读并保证 UTF-8 边界。hunk 作为索引行保留标题，旧新行号按真实 patch 后台计算；未跟踪内容不按 patch 标记解释。重命名/权限变更可返回零正文行。

桌面层后续采用单个活动差异文档槽和请求代次，绑定 repositoryId/snapshotId/changeId/side/contextLines；切文件、重新加载上下文、刷新、切仓库及退出释放旧槽。在队列内生成，完成后复核代次，页读取只访问已签发文档且不能请求路径。前端后续复用 TanStack Virtual 与有界页缓存，渲染实际可见行，红删绿增、双行号、长行续读和上下文加载维持原有语义，不将旧全文解析留在大差异路径。

先验收独立核心：至少 10,000 差异行的末页/编号/统计、比较侧隔离、未跟踪 patch 样式原文、无末尾换行、UTF-8 长行及页容量/非法请求；再接桌面槽失效与迟到返回测试、前端虚拟 DOM 和 Release 实机。核心实现完成不等同于新接口或产品界面已接入。

差异桌面接口细化：`open_diff_document(repositoryId,snapshotId,changeId,side,contextLines)` 返回 `kind=paged` 的 documentId 和摘要，或 binary/unsupported；`read_diff_page(repositoryId,snapshotId,documentId,startRow,count,byteOffset)` 返回核心有界页；`close_diff_document(repositoryId,snapshotId,documentId)` 仅释放匹配槽。旧 read_file_diff 保持兼容。打开请求在主锁内先分配代次并清理旧槽，再在仓库队列生成；队列开始和发布均检查 Context/请求代次。页请求捕获 Arc 并在返回前检查仍为当前槽，刷新/新打开会拒绝旧页。迟到关闭不能删除新文档。单槽正文预算最大 16 MiB，响应不返回整段 content。

差异前端接线：仓库控制器负责打开摘要及释放能力，切文件/刷新/清空/关闭详细面板时释放当前差异，迟到的打开成功也主动关闭。新分页视图按文档 ID 重建缓存；最多八页，每页最多 100 行且沿用后台 256 KiB 限制，串行读取并优先最新视口。固定 28px 行、最大 960px 视口、相邻预读；文本直接渲染，保留增删背景和真实双行号。长行续段替换原段并可回到首段。发生错误停止自动读，用户从原文件选择入口重开。当前短文档折叠语义需在新分页视图接入后逐项复核，不以虚拟滚动代替折叠验收。

分页上下文折叠：摘要增加 folds[{startRow,count}]，仅索引超过八行的连续 context，不跨 hunk、增删或无末尾换行注释；默认保留两端各三行，中间显示可展开/收起按钮，统计不受折叠影响。前端只构造轻量行位置映射，读取可见原始行，跳过折叠正文。由于一个视口可跨多个不连续段，缓存调整为最多 128 行/2 MiB 正文字节（与原八页总字节预算一致），避免以页数量淘汰时在多折叠段之间反复补读。最大请求视口 96 行，串行读取相邻缺失行，长行续段仍替换当前行。

恢复阶段验收补齐：交接中的本地忽略前端测试未随仓库迁移，不能以旧机器的通过记录替代本机执行。按现有 remoteSyncController 合同重建同步关系、失败停止、异步分支变化、首次上游/发布及部分成功提示用例；使用注入 API 的纯控制器测试，不操作真实用户远端。原生目录选择器待人工核对期间继续此独立验证工作。

### 退出入口保护补齐（2026-09-24）

代码复核发现窗口关闭监听仅覆盖文件编辑与终端，遗漏设置和冲突草稿。复用已有确认框：先处理文件草稿，再处理设置草稿或冲突草稿，最后进入终端结束确认；取消任一确认中止退出，设置保存失败保持草稿。设置加载/保存或 Git 写任务进行中阻止关闭。此项不改变 Git 写接口，不扩大为系统强制终止保护。验收包含单独设置、单独冲突、文件与设置并存的取消/继续路径；Dock 退出仍须实机单独验证。

### 设置长表单操作栏（2026-09-24）

完整工作台浏览器夹具在 860×620 切换终端分类时，应用按钮位于 y=1234..1274，超出 620 高度视口，违背第 2.7 节底部操作固定可见要求。将设置字段改为独立滚动区，操作栏位于滚动区外；弹窗高度有界，分类区域可单独滚动，不改变设置提交或取消逻辑。首次底部粘附方案在键盘验证中遮挡字段，已替换，不作为交付方案。验收覆盖终端长表单顶部/中段/底部和键盘到达取消/应用，区分浏览器与原生结果。

低高度补充：以 430×310 CSS 布局空间检查 860×620 在 200% 下的重排条件，字段区被标题、间距和换行操作按钮压至零高度。对高度不超过 420px 的设置弹窗减少边缘留白与标题/操作区间距，保留字段滚动空间、完整按钮文案和分类滚动；这只是重排条件验证，系统原生缩放事件仍需实机验收。

### 2026-09-24 恢复验收阻塞检查

2026-09-25 原生同步夹具调整：生产拒绝本地路径传输，原 bare 仓库保留，复用系统 Apache、Git http-backend 和 OpenSSL 在 127.0.0.1 非特权端口提供隔离 HTTPS 测试服务。证书校验保持开启；测试 CA 仅通过测试进程专用 XDG Git 全局配置按该 HTTPS 地址加载，不写用户 Git 配置或系统钥匙串。服务只暴露专用临时夹具，结束后停止。验收分别核对四种分支关系的 UI 结果、真实 refs 与干净工作区；该本地 TLS 测试不替代真实托管服务认证验收。

2026-09-25 同步推送后状态修复：原生 ahead 验收确认远端已推进但本地 tracking 仍旧。保持后端私有推送不写本地 refs 的契约，在前端 syncPush 成功后复用首次发布已有的 readRemotes→fetchAll，重新签发远端 ID，核对原仓库/分支/提交与目标远端名称，再由现有获取流程更新真实跟踪引用和界面。推送成功但本地刷新、补充获取或后续核验失败时保留已推送事实并停止，绝不自动重推。equal/behind/diverged 不增加获取。验收包含请求顺序、新 ID、补充获取失败、刷新失败、分支变化、迟到响应，以及 Release 推送后 tracking 与图标签一致。

2026-09-25 本地修改低高度布局：底栏展开后列表外层仅约 81px，虚拟列表仍最少 120px；提交区同时单独限高滚动，导致嵌套裁切及表单难以定位。只调整本地修改正文容器：标题保留，列表与提交区共同位于可滚动正文，列表保持有界虚拟视口和最小可操作高度，提交区不再单独限高；不改暂存/提交业务。验收覆盖底栏展开、860×620、正常高度、键盘定位列表/提交说明/按钮及首末项导航，确保控件通过滚动可达且不相互遮挡。

上条布局实施须同时满足第 2.4 节原有“按钮同一行固定可见”：文件列表和提交说明在滚动正文内，已暂存数量、选择原因和两个动作按钮放在正文外的固定操作栏。此前将动作一起滚动仅达到可达性，不能视为该需求完成；复测必须检查正文顶部和底部两种滚动位置下按钮边界均位于抽屉内。

固定操作栏复核发现，底栏原高度上限仅给主工作区保留 240px，小窗口及消息区并存时正文降至约 29px。将上限计算预留空间提高到 360px，仍保留底栏最小 120px，并同步拖动/键盘及窗口缩小后的相同边界；底栏会自动缩小以保留主操作区，不销毁终端或记录。

本轮仅勾选已由当前源码和实际测试支持的实现/文档条目；任务整体与其原生验收仍未完成。最新 Release 已包含设置长表单与低高度布局。原生工具连续多轮明确报告 Mac 已锁定、无法自动解锁，已请求用户解锁；重复构建或重复浏览器 mock 不能弥补此证据缺口。

恢复入口：解锁后先处理旧进程的目录选择器并正常退出，启动最新 Release；从专用临时仓库继续原生退出（含 Dock）、组合草稿、终端输入与尺寸/持续输出、设置持久化/路径选择、外部应用打开、同步流程和大型文件/差异/目录性能测量。真实认证与 Windows 平台另需可用环境，不沿用历史跳过为发布通过。原始范围不缩减，不新增发布、提交或远端写入授权。
